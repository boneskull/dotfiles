export PATH="$PATH:./node_modules/.bin:/usr/local/bin:/usr/local/sbin:/usr/local/heroku/bin"
[[ -n `command -v brew` && -n `brew ls coreutils` ]] && {
  export PATH="$PATH:$(brew --prefix coreutils)/libexec/gnubin"
}

#!/bin/bash
#
# to enable this file, "sudo defaults write com.apple.loginwindow LoginHook /path/to/this/script"

VAGRANT_DIR=${HOME}/projects/beacon/
[[ -d ${VAGRANT_DIR} ]] && \
  cd ${VAGRANT_DIR} && \
  /usr/bin/env vagrant suspend

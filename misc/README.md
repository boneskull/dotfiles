# Random Shit

This directory contains files that should **not** be symlinked into `$HOME` but I want to keep handy.

## Files

- `main.json` - Standard GitHub ruleset configuration. Exported from [modestbench](https://github.com/boneskull/modestbench) but can be imported into any repository to protect the `main` branch.
